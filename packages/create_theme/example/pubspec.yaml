name: example
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  create_theme_annotation:
    git: 
      url: https://github.com/SuperPenguin/create_theme.git
      path: packages/create_theme_annotation
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  build_runner: ^2.2.0
  create_theme:
    git: 
      url: https://github.com/SuperPenguin/create_theme.git
      path: packages/create_theme
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

dependency_overrides:
  create_theme:
    path: ../../create_theme
  create_theme_annotation:
    path: ../../create_theme_annotation

flutter:
  uses-material-design: true
